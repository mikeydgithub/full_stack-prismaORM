// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

// build datasource 
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// build model
model GroceryList {
  id Int @id @default(autoincrement())
  title String
  checked Boolean? @default(false)
}

// with a defined schema we can run our first migration
// use npx prisma migrate dev --name init
// next install the primsa client npm install @prisma/client